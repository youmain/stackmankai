# ポーカールール実装の確認レポート

## 実施日時
2025-12-17 16:30 JST

## 確認項目

### 1. ゲームフロー

#### ✅ ゲーム開始
- 2人以上のプレイヤーが必要
- ディーラーボタン、スモールブラインド、ビッグブラインドの設定
- 各プレイヤーに2枚のホールカードを配布

#### ✅ ベッティングラウンド
- **Preflop**: ホールカード配布後
- **Flop**: 3枚のコミュニティカード公開後
- **Turn**: 4枚目のコミュニティカード公開後
- **River**: 5枚目のコミュニティカード公開後
- **Showdown**: 最終ベッティング後、ハンド比較

### 2. プレイヤーアクション

#### ✅ Fold（フォールド）
- 常に可能
- ハンドを捨てる

#### ✅ Check（チェック）
- `player.currentBet >= game.currentBet`の場合のみ可能
- ベットせずに次のプレイヤーにターンを渡す

#### ✅ Call（コール）
- `game.currentBet > player.currentBet`の場合のみ可能
- 現在のベット額に合わせる
- コール額 = `game.currentBet - player.currentBet`

#### ✅ Bet（ベット）
- `game.currentBet === 0`の場合のみ可能
- 最初にベットする
- 最小ベット額 = `game.minRaise`

#### ⚠️ Raise（レイズ）
- `game.currentBet > 0`の場合のみ可能
- 既存のベットを上回る額をベット
- 最小レイズ額 = `game.minRaise`

**問題点（修正済み）:**
- UIが`currentPlayer.currentBet`を使ってボタン表示を判定していた
- 正しくは`game.currentBet`を使うべき
- バリデーションロジックは正しく実装されていた

#### ✅ All-in（オールイン）
- 常に可能（スタックがある場合）
- 全スタックをベット

### 3. ベット額の計算

#### ✅ Pot（ポット）
- 全プレイヤーのベット額の合計
- アクションごとに更新

#### ✅ Current Bet（現在のベット額）
- 現在のラウンドで最も高いベット額
- レイズやベットで更新

#### ✅ Min Raise（最小レイズ額）
- 通常はビッグブラインドの額
- レイズがあった場合は、そのレイズ額

### 4. ゲーム進行

#### ✅ ターン管理
- `currentPlayerIndex`で現在のプレイヤーを追跡
- アクション後、次のアクティブプレイヤーに移動
- フォールドまたはオールインのプレイヤーはスキップ

#### ✅ ラウンド終了判定
- 全プレイヤーが同じ額をベットした
- または、1人を除いて全員フォールド

#### ✅ フェーズ進行
- waiting → preflop → flop → turn → river → showdown

### 5. ハンド評価

#### ✅ ハンドランキング
1. Royal Flush（ロイヤルフラッシュ）
2. Straight Flush（ストレートフラッシュ）
3. Four of a Kind（フォーカード）
4. Full House（フルハウス）
5. Flush（フラッシュ）
6. Straight（ストレート）
7. Three of a Kind（スリーカード）
8. Two Pair（ツーペア）
9. One Pair（ワンペア）
10. High Card（ハイカード）

#### ✅ ショーダウン
- 残っているプレイヤーのハンドを比較
- 最も強いハンドが勝利
- 同じ強さの場合は引き分け（ポット分割）

### 6. タイムアウト処理

#### ✅ タイムアウト設定
- デフォルト: 30秒
- `turnStartTime`でターン開始時刻を記録

#### ✅ タイムアウト時の処理
- 自動的にフォールド
- 連続タイムアウト回数を記録
- 3回連続でタイムアウトした場合、自動的に席を立つ

### 7. ブラインド

#### ✅ スモールブラインド
- ディーラーの左隣
- ビッグブラインドの半額

#### ✅ ビッグブラインド
- スモールブラインドの左隣
- 最小ベット額

## 発見された問題と修正

### ❌ 問題1: ベット/レイズボタンの表示が間違っている

**症状:**
- 他のプレイヤーがベットした後、自分のターンで「ベット」ボタンが表示される
- 「ベット」ボタンを押すと「レイズしてください」というエラーが表示される

**原因:**
```typescript
// 修正前（poker-table.tsx 503行目）
{currentPlayer.currentBet === 0 ? "ベット" : "レイズ"}
```
- `currentPlayer.currentBet`は自分の現在のベット額
- 他のプレイヤーがベットしても、自分がまだベットしていなければ0
- そのため「ベット」と表示されてしまう

**修正:**
```typescript
// 修正後
{game.currentBet === 0 ? "ベット" : "レイズ"}
```
- `game.currentBet`は現在のラウンドで最も高いベット額
- 他のプレイヤーがベットしていれば0より大きい
- そのため正しく「レイズ」と表示される

**修正箇所:**
- `components/poker/poker-table.tsx` 496行目: アクション送信
- `components/poker/poker-table.tsx` 503行目: ボタン表示

## テストケース

### テストケース1: 誰もベットしていない
- **状態**: `game.currentBet === 0`
- **期待**: 「ベット」ボタンが表示される
- **結果**: ✅ 正しく動作

### テストケース2: 他のプレイヤーがベットした
- **状態**: `game.currentBet > 0`, `currentPlayer.currentBet === 0`
- **期待**: 「レイズ」ボタンが表示される
- **結果**: ✅ 修正後、正しく動作

### テストケース3: 自分がベットして、他のプレイヤーがレイズした
- **状態**: `game.currentBet > currentPlayer.currentBet > 0`
- **期待**: 「レイズ」ボタンが表示される
- **結果**: ✅ 修正後、正しく動作

## 結論

### ✅ ポーカールールの実装は正しい

バリデーションロジック（`lib/poker-logic/validation.ts`）は正しく実装されており、以下のルールが守られています：

1. ベットは`game.currentBet === 0`の場合のみ可能
2. レイズは`game.currentBet > 0`の場合のみ可能
3. チェックは`player.currentBet >= game.currentBet`の場合のみ可能
4. コールは`game.currentBet > player.currentBet`の場合のみ可能

### ✅ UI問題を修正

UIが間違った変数（`currentPlayer.currentBet`）を使ってボタン表示を判定していた問題を修正しました。修正後は、正しく`game.currentBet`を使ってボタン表示とアクション送信を行います。

## 次のステップ

1. ビルドとデプロイ
2. 本番環境でテスト
3. ユーザーに報告
