# データマイグレーション実行ガイド

## ⚠️ 重要な注意事項

**このマイグレーションは本番データに影響を与えます。必ず以下の手順を守ってください。**

1. **バックアップを取得してから実行**
2. **DRY RUNモードで確認してから実行**
3. **メンテナンスモードで実行**（ユーザーアクセスを停止）
4. **ピーク時間を避けて実行**

---

## 📋 事前準備チェックリスト

### ✅ 1. 環境確認
- [ ] Node.js 18以上がインストールされている
- [ ] Firebase Admin SDKの認証情報が設定されている
- [ ] 本番環境のFirebaseプロジェクトにアクセスできる

### ✅ 2. バックアップ
```bash
# Firestoreのバックアップを取得（Firebase Console または gcloud CLI）
gcloud firestore export gs://[YOUR_BUCKET_NAME]/backups/$(date +%Y%m%d_%H%M%S)
```

### ✅ 3. メンテナンスモード
- [ ] ユーザーへの事前通知（24時間前）
- [ ] アプリケーションをメンテナンスモードに設定
- [ ] すべてのユーザーがログアウトしたことを確認

---

## 🚀 マイグレーション実行手順

### Step 1: DRY RUN（確認のみ）

```bash
cd /home/ubuntu/stackmankai

# 依存関係のインストール
npm install

# DRY RUNモードで実行（データは変更されません）
npm run migrate:store-isolation -- --dry-run
```

**確認事項:**
- [ ] 移行対象のデータ件数が正しい
- [ ] エラーが発生していない
- [ ] 各店舗のデータ数が正しい

### Step 2: 本番マイグレーション

```bash
# 本番環境のFirebase認証情報を設定
export GOOGLE_APPLICATION_CREDENTIALS="/path/to/serviceAccountKey.json"

# マイグレーション実行
npm run migrate:store-isolation

# 進捗を確認
# ログに以下のような出力が表示されます:
# ✓ Players: 150件を3店舗に移行完了
# ✓ Games: 500件を3店舗に移行完了
```

**実行中の注意:**
- プロセスを中断しないでください
- エラーが発生した場合は、ログを保存してください
- 完了まで約10-30分かかります（データ量による）

### Step 3: マイグレーション結果の確認

```bash
# マイグレーション状況を確認
npm run migrate:store-isolation -- --check-status
```

**確認事項:**
- [ ] 新しいコレクション構造にデータが移行されている
  - `players/store_{storeId}/players`
  - `games/store_{storeId}/games`
- [ ] データ件数が一致している
- [ ] 各店舗のデータが正しく分離されている

### Step 4: アプリケーションのテスト

```bash
# 開発環境で新しいコードをテスト
npm run dev

# 以下の機能をテスト:
# - プレイヤー一覧の表示
# - プレイヤーの追加・編集・削除
# - ゲームの作成・管理
# - 顧客認証
```

**テスト項目:**
- [ ] 各店舗のプレイヤーが正しく表示される
- [ ] 店舗間でデータが混在していない
- [ ] プレイヤーの追加・編集・削除が正常に動作する
- [ ] ゲームの作成・管理が正常に動作する
- [ ] 顧客認証が正常に動作する

### Step 5: 古いデータの削除（オプション）

**⚠️ 警告: この操作は元に戻せません。必ずバックアップを確認してから実行してください。**

```bash
# 古いコレクションを削除
npm run migrate:store-isolation -- --cleanup-old-data

# 確認プロンプトが表示されます:
# "古いデータを削除しますか？この操作は元に戻せません。(yes/no)"
```

---

## 🔄 ロールバック手順

マイグレーションに問題が発生した場合:

### 方法1: バックアップから復元

```bash
# Firestoreのバックアップから復元
gcloud firestore import gs://[YOUR_BUCKET_NAME]/backups/[BACKUP_TIMESTAMP]
```

### 方法2: 古いコードに戻す

```bash
# 古いコードに戻す（マイグレーション前のコミット）
git revert HEAD

# デプロイ
npm run deploy
```

---

## 📊 マイグレーション後の確認項目

### データ整合性チェック

```bash
# データ整合性を確認するスクリプトを実行
npm run check:data-integrity
```

**確認項目:**
- [ ] 全プレイヤーが正しい店舗に属している
- [ ] 全ゲームが正しい店舗に属している
- [ ] 顧客アカウントが正しい店舗にリンクされている
- [ ] データの欠損がない

### パフォーマンスチェック

- [ ] ページの読み込み速度が遅くなっていない
- [ ] Firestoreの読み取り/書き込み回数が増加していない
- [ ] エラーログに異常がない

---

## 🆘 トラブルシューティング

### エラー: "Permission denied"

**原因:** Firebase Admin SDKの認証情報が正しく設定されていない

**解決方法:**
```bash
export GOOGLE_APPLICATION_CREDENTIALS="/path/to/serviceAccountKey.json"
```

### エラー: "Document already exists"

**原因:** マイグレーションが既に実行されている

**解決方法:**
```bash
# マイグレーション状況を確認
npm run migrate:store-isolation -- --check-status

# 必要に応じて、古いデータを削除してから再実行
```

### エラー: "Timeout"

**原因:** データ量が多すぎる

**解決方法:**
- バッチサイズを小さくする（スクリプト内の`BATCH_SIZE`を変更）
- 店舗ごとに分けて実行する

---

## 📞 サポート

問題が発生した場合:
1. エラーログを保存
2. マイグレーション状況を確認
3. バックアップから復元を検討

---

## 🎉 完了後

マイグレーションが完了したら:

1. ✅ メンテナンスモードを解除
2. ✅ ユーザーに通知
3. ✅ 24時間監視
4. ✅ バックアップを保持（最低7日間）

---

**最終更新:** 2025年12月23日
