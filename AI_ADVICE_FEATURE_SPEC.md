# AIアドバイス機能：詳細仕様書

## 概要
この機能は、単なるルール説明ではなく、**対戦相手のプレイスタイル分析**に基づいた戦略的なアドバイスを提供することを核としています。

---

## 1. 機能の核：対戦相手のプレイスタイル分析

AIは自分のハンドの強さだけでなく、対戦相手のこれまでの行動履歴から以下の指標を算出・分析します。

### 分析指標

| 指標 | 内容 | 分析結果の活用 |
|------|------|--------------|
| **VPIP** | 参加率（どれだけ多くのハンドでチップを投じているか） | ルース（広い）かタイト（狭い）かの判定 |
| **PFR** | プリフロップでのレイズ率 | 攻撃性の判定 |
| **AF (Aggression Factor)** | 攻撃的指標（（ベット＋レイズ）÷ コール） | ブラフの頻度や、強い時だけ打つタイプかの判定 |
| **行動パターン** | チェックレイズの頻度、ドンクベットの有無など | 特定のシチュエーションでの癖の特定 |

---

## 2. アドバイスの構成要素

ユーザーに対して、以下の3つの視点からアドバイスを提示します。

### ① 現状分析（状況把握）
- 現在のポットオッズと、自分のハンドが勝っている確率（エクイティ）の提示
- 「相手は現在、非常に攻撃的なレンジで動いています」といった状況説明

### ② 相手のレンジ推測
- 「相手のこれまでのVPIPが低いため、このレイズは非常に強いハンド（AA, KK等）の可能性が高いです」といった推測

### ③ 推奨アクションと根拠
- **推奨**: フォールド / コール / レイズ（額の提示）
- **根拠**: 「相手のAFが低いため、ここでのチェックレイズに対してはフォールドが期待値的に正解です」といった戦略的理由

---

## 3. 実装に向けたデータ構造（Firestore）

### playerStats コレクション
```
/playerStats/{playerId}
├── vpip: number (参加率)
├── pfr: number (プリフロップレイズ率)
├── af: number (アグレッションファクター)
├── totalHands: number (総ハンド数)
├── participatedHands: number (参加したハンド数)
├── raisedHands: number (レイズしたハンド数)
├── checkRaiseCount: number (チェックレイズ回数)
├── dunkBetCount: number (ドンクベット回数)
├── updatedAt: timestamp
└── actionHistory: array (行動履歴の詳細)
```

### currentHandHistory
```
/pokerGames/store_{storeId}/games/{gameId}/handHistory
├── handNumber: number
├── players: array
│   ├── playerId: string
│   ├── actions: array
│   │   ├── phase: string (preflop, flop, turn, river)
│   │   ├── action: string (fold, check, call, bet, raise)
│   │   ├── amount: number
│   │   └── timestamp: timestamp
│   └── finalStack: number
└── result: object
    ├── winners: array
    ├── pot: number
    └── timestamp: timestamp
```

---

## 4. AIエンジン（LLM）の役割

### 数値計算
- ポットオッズやエクイティの計算
- これはプログラム側で計算し、AIに渡す

### 言語化
- 計算結果と統計データを元に、対戦相手の性格を考慮した人間味のある戦略アドバイスを生成

---

## 5. 実装フロー

### Phase 1: データ収集の仕組み
1. 各ハンド終了時に、全プレイヤーの行動をcurrentHandHistoryに記録
2. ゲーム終了時に、playerStatsを集計・更新

### Phase 2: 統計指標の計算
1. VPIP = 参加したハンド数 / 総ハンド数
2. PFR = プリフロップでレイズしたハンド数 / 総ハンド数
3. AF = (ベット数 + レイズ数) / コール数

### Phase 3: AIアドバイスの生成
1. 現在のゲーム状態を取得
2. 相手プレイヤーのplayerStatsを参照
3. 統計データとハンド情報をLLMに渡す
4. LLMが戦略的アドバイスを生成

---

## 6. 引き継ぎ時のポイント

新しいチャットでは、以下のように伝えると、アドバイス機能の実装が非常にスムーズになります：

> 「以前の巨大なロジックファイルをベースに再構築する際、この統計データ（VPIP等）を収集する仕組みを最初から組み込みたい」

---

## 7. 実装優先度

1. **データ構造の設計と実装** - playerStats、currentHandHistoryの構造を確定
2. **データ収集ロジック** - 各ハンド終了時に行動データを記録
3. **統計指標の計算** - VPIP、PFR、AFの計算ロジック
4. **AIアドバイス生成** - LLMとの連携、プロンプト設計
5. **UI/UX** - アドバイス表示画面の設計

---

## 8. 注意事項

- 統計データは十分なハンド数（最低50ハンド以上）が必要
- 新規プレイヤーの場合は、デフォルト値またはジェネリックなアドバイスを提供
- プライバシー保護：プレイヤーの詳細な行動データは、本人と管理者のみがアクセス可能
