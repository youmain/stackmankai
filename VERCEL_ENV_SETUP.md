# Vercel環境変数設定手順

## 開発用リセットボタンを表示するための設定

ポーカーゲームの開発用リセットボタンを本番環境で表示するには、Vercelで環境変数を設定する必要があります。

### 手順

1. **Vercelダッシュボードにアクセス**
   - https://vercel.com にアクセス
   - プロジェクト「stackmankai」を選択

2. **環境変数設定ページに移動**
   - 「Settings」タブをクリック
   - 左側のメニューから「Environment Variables」を選択

3. **新しい環境変数を追加**
   - 「Add New」ボタンをクリック
   - 以下の情報を入力：
     - **Name:** `NEXT_PUBLIC_SHOW_DEV_TOOLS`
     - **Value:** `true`
     - **Environment:** `Production` にチェック

4. **変更を保存**
   - 「Save」ボタンをクリック

5. **再デプロイ**
   - 「Deployments」タブに移動
   - 最新のデプロイメントの右側にある「...」メニューをクリック
   - 「Redeploy」を選択
   - 確認ダイアログで「Redeploy」をクリック

### 確認方法

再デプロイが完了したら、以下の手順で確認：

1. https://stackmankai-zeta.vercel.app/customer-view にアクセス
2. ログイン後、ポーカーゲーム画面の下部に「🛠️ 開発用: ゲームをリセット」ボタンが表示されることを確認

### 注意事項

- `NEXT_PUBLIC_` プレフィックスが付いた環境変数は、クライアント側（ブラウザ）で使用できます
- 本番環境でこのボタンを非表示にする場合は、環境変数を削除するか、値を `false` に変更してください
- ローカル開発環境では `.env.local` ファイルで自動的に有効になります

### 代替方法（環境変数を設定しない場合）

環境変数を設定せずにリセット機能を使用する場合：

1. ブラウザのコンソールを開く（F12キー）
2. 以下のコードを実行：
   ```javascript
   // Firestoreのゲームドキュメントを直接リセット
   // （実装が必要）
   ```

ただし、この方法は推奨されません。環境変数を使用することをお勧めします。
