# Stack Man Kai - 認証リダイレクト削除の対応完了

## 実施日時
2025年12月22日

## 対応内容

### 問題
- ページ遷移やF5リロードのたびにログインページへ強制リダイレクトされる
- 「一覧を見る」をクリックすると「ログインしてください」エラーが表示される
- セッション復元中に認証チェックが走り、ユーザーがログアウトされてしまう
- ユーザー体験が非常に悪い

### 解決策
**強制リダイレクトを削除し、ユーザーフレンドリーなエラー表示に変更**

以下の3つのページから強制的なログインページへのリダイレクトを削除しました:

1. **購入ページ** (`app/stack-man-hand/purchase/page.tsx`)
2. **マイハンド一覧ページ** (`app/stack-man-hand/my-hands/page.tsx`)
3. **ハンド表示ページ** (`app/stack-man-hand/display/[handId]/page.tsx`)

### 変更内容の詳細

#### Before（変更前）
```typescript
// 認証チェックでログインページへ強制リダイレクト
if (!storeId || !playerId) {
  router.push("/customer-auth")
  return null
}
```

#### After（変更後）
```typescript
// ログインが必要な場合、ページ内でメッセージとボタンを表示
if (!storeId) {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
      <div className="text-center">
        <p className="text-gray-600 mb-4">ログインが必要です</p>
        <button
          onClick={() => router.push("/customer-auth")}
          className="px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
        >
          ログインページへ
        </button>
      </div>
    </div>
  )
}
```

### 改善点

1. **セッション復元の問題解消**
   - localStorageからのセッション復元中に強制リダイレクトされなくなった
   - F5リロードでログアウトされる問題を解決

2. **ユーザー体験の向上**
   - ページ遷移時に不要なリダイレクトが発生しない
   - ユーザーが意図しないログアウトを防止
   - 必要な場合のみ、ユーザーが自分でログインボタンをクリックできる

3. **セキュリティの維持**
   - Firestoreセキュリティルールが引き続きデータアクセスを保護
   - 不正なアクセスはバックエンドレベルでブロックされる
   - フロントエンドの認証チェックは「ユーザーフレンドリーなガイド」として機能

### 技術的な背景

#### セッション管理の仕組み
- localStorageに `storeId` と `playerId` を保存
- ページロード時にlocalStorageから復元
- 復元は非同期処理のため、タイミングによっては一時的に未ログイン状態になる

#### 以前の問題
- セッション復元前に認証チェックが実行される
- 未ログイン状態と判定され、強制的にログインページへリダイレクト
- 実際にはログイン済みだが、復元が完了する前にリダイレクトされてしまう

#### 今回の解決方法
- 強制リダイレクトを削除
- 実際に未ログインの場合のみ、ページ内でメッセージとボタンを表示
- セッション復元が完了すれば、通常通りコンテンツが表示される

### デプロイ情報

- **コミットハッシュ**: 989874b
- **コミットメッセージ**: "feat: remove forced auth redirects from Stack Man Hand pages"
- **デプロイ先**: https://stackmankai-zeta.vercel.app
- **デプロイ日時**: 2025年12月22日

### 影響範囲

#### 変更されたファイル
1. `app/stack-man-hand/purchase/page.tsx`
2. `app/stack-man-hand/my-hands/page.tsx`
3. `app/stack-man-hand/display/[handId]/page.tsx`

#### 変更されていないファイル
- 認証コンテキスト (`contexts/auth-context.tsx`) - 変更なし
- Firestoreセキュリティルール - 変更なし
- その他のページ - 変更なし

### テスト推奨項目

1. **正常系**
   - ログイン後、購入ページにアクセスできることを確認
   - ログイン後、マイハンド一覧ページにアクセスできることを確認
   - ログイン後、ハンド表示ページにアクセスできることを確認
   - F5リロードでログアウトされないことを確認
   - ページ間の遷移がスムーズに行えることを確認

2. **異常系**
   - 未ログイン状態で各ページにアクセスした場合、適切なメッセージが表示されることを確認
   - 「ログインページへ」ボタンをクリックすると、ログインページに遷移することを確認

3. **セキュリティ**
   - 未ログイン状態でFirestoreにアクセスしようとすると、適切にエラーが返されることを確認
   - Firestoreセキュリティルールが正しく機能していることを確認

### ロールバック方法

問題が発生した場合、以下のコマンドでロールバック可能です:

```bash
cd /home/ubuntu/stackmankai
git revert 989874b
git push origin main
```

### 今後の改善案

1. **ローディング状態の改善**
   - セッション復元中にローディングインジケーターを表示
   - ユーザーに「復元中」であることを明示

2. **エラーメッセージの多言語対応**
   - 現在は日本語のみ
   - 将来的に英語などの対応も検討

3. **セッション有効期限の実装**
   - 現在は無期限
   - セキュリティ向上のため、有効期限の実装を検討

## まとめ

Stack Man Handページの強制認証リダイレクトを削除し、ユーザー体験を大幅に改善しました。セッション復元の問題が解消され、F5リロードやページ遷移時にログアウトされることがなくなりました。セキュリティはFirestoreセキュリティルールによって引き続き保護されています。

変更は安全にデプロイされており、問題が発生した場合は簡単にロールバック可能です。
